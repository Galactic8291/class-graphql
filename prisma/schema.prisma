generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model teacher {
  uuid   String   @id @default(dbgenerated())
  name   String
  grades String[]
}
